# node-rest-api-server-practice
REST API Server based on Node.js & TDD
* version1 : non-DB
* version2 : DB with sqlite3 

## Execute
```
$ npm i

$ npm start // start server
$ curl localhost:3000/users
$ curl localhost:3000/users/{id}

$ npm t // TDD
```
